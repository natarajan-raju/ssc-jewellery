{
  "name": "ssc-jewellery-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "cd client && npm install && cd ../server && npm install",
    "build": "npm run install-all && cd client && npm run build",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}